{
  "name": "devxhub-playwright-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --reporter=list",
    "test:headed": "playwright test --headed",
    "test:report": "playwright test --reporter=list && node reports/generate-enhanced-report.js",
    "install:browsers": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1",
    "typescript": "^5.4.5"
  }
}


